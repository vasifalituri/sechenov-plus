# 🚀 Настройка Email на Vercel (Production)

## Проблема:
✅ На localhost (http://localhost:3000) - работает  
❌ На Vercel (https://sechenov-plus.vercel.app) - НЕ работает  

**Причина:** Переменные окружения (GMAIL_USER, GMAIL_APP_PASSWORD) есть только в `.env.local` локально, но их нет на Vercel!

---

## ✅ Решение: Добавить переменные на Vercel

### Шаг 1: Откройте настройки проекта на Vercel

1. Перейдите: **https://vercel.com/dashboard**
2. Выберите проект: **sechenov-plus**
3. Нажмите **Settings** (вверху)
4. Слева выберите **Environment Variables**

---

### Шаг 2: Добавьте переменные Gmail SMTP

Добавьте **2 переменные**:

#### Переменная 1: GMAIL_USER

| Поле | Значение |
|------|----------|
| **Name** | `GMAIL_USER` |
| **Value** | `vasifalituri.aze.pak@gmail.com` |
| **Environment** | ✅ Production<br>✅ Preview<br>✅ Development |

Нажмите **Save**

---

#### Переменная 2: GMAIL_APP_PASSWORD

| Поле | Значение |
|------|----------|
| **Name** | `GMAIL_APP_PASSWORD` |
| **Value** | `mhfmbnvopjhyijid` |
| **Environment** | ✅ Production<br>✅ Preview<br>✅ Development |

Нажмите **Save**

---

### Шаг 3: Передеплойте проект

После добавления переменных нужно **передеплоить**:

**Способ 1: Через Vercel Dashboard**
1. Перейдите на вкладку **Deployments**
2. Найдите последний деплой
3. Нажмите **⋯** (три точки) → **Redeploy**
4. Нажмите **Redeploy** для подтверждения

**Способ 2: Через Git (если проект подключен к GitHub)**
```bash
git commit --allow-empty -m "Trigger redeploy for env vars"
git push
```

**Способ 3: Через Vercel CLI**
```bash
vercel --prod
```

---

### Шаг 4: Проверьте работу

После деплоя (займет 1-2 минуты):

1. Откройте: **https://sechenov-plus.vercel.app/register**
2. Зарегистрируйте пользователя с реальным email
3. Проверьте email - код должен прийти!

---

## 📸 Скриншот настроек Vercel:

```
┌─────────────────────────────────────────────────────┐
│ Environment Variables                                │
├─────────────────────────────────────────────────────┤
│                                                      │
│ Name: GMAIL_USER                                    │
│ Value: vasifalituri.aze.pak@gmail.com              │
│ Environments: ☑ Production ☑ Preview ☑ Development │
│                                                      │
│ Name: GMAIL_APP_PASSWORD                            │
│ Value: mhfmbnvopjhyijid                             │
│ Environments: ☑ Production ☑ Preview ☑ Development │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 🔐 Безопасность:

✅ **Безопасно:** Environment Variables на Vercel зашифрованы  
✅ **Не видны в коде:** Они не попадают в Git репозиторий  
✅ **Доступны только вашему проекту**  

---

## 🐛 Проверка после деплоя:

### Проверить через Vercel Logs:

1. Откройте **Deployments** → выберите последний деплой
2. Нажмите **View Function Logs**
3. Попробуйте зарегистрироваться на сайте
4. В логах должно появиться:
   ```
   ✅ Email sent successfully
   📧 To: user@example.com
   ```

### Если не работает - проверьте:

1. **Переменные добавлены правильно?**
   - Имена: `GMAIL_USER` и `GMAIL_APP_PASSWORD` (без опечаток)
   - Значения скопированы правильно

2. **Проект передеплоен?**
   - После добавления переменных ОБЯЗАТЕЛЬНО нужен redeploy!

3. **Проверьте логи в Vercel:**
   - Ошибка "Gmail SMTP not configured" → переменные не добавлены
   - Ошибка "Invalid login" → неверный пароль
   - Ошибка отсутствует, но письмо не приходит → проверьте спам

---

## 📋 Полный список переменных для Vercel:

Убедитесь что у вас есть все эти переменные:

```env
# Database
DATABASE_URL="postgresql://..."

# NextAuth
NEXTAUTH_URL="https://sechenov-plus.vercel.app"
NEXTAUTH_SECRET="your-secret"

# Email (НОВЫЕ!)
GMAIL_USER="vasifalituri.aze.pak@gmail.com"
GMAIL_APP_PASSWORD="mhfmbnvopjhyijid"

# Supabase
NEXT_PUBLIC_SUPABASE_URL="https://..."
NEXT_PUBLIC_SUPABASE_ANON_KEY="..."
SUPABASE_SERVICE_ROLE_KEY="..."

# Storage
SUPABASE_STORAGE_BUCKET="materials"
SUPABASE_PROFILES_BUCKET="profiles"

# Mega (если используется)
MEGA_EMAIL="..."
MEGA_PASSWORD="..."
```

---

## ⏱️ Время деплоя:

- **Добавление переменных:** 2 минуты
- **Redeploy:** 1-2 минуты
- **Проверка:** 1 минута

**Итого:** ~5 минут и все заработает! 🚀

---

## ❓ Частые вопросы:

### Q: Нужно ли коммитить .env.local в Git?

**A:** ❌ **НЕТ!** `.env.local` должен быть в `.gitignore`. Переменные добавляются только через Vercel Dashboard.

### Q: Можно ли использовать разные email для production и preview?

**A:** ✅ Да! Можно добавить разные значения для разных окружений в Vercel.

### Q: Что если забыл App Password?

**A:** Создайте новый на https://myaccount.google.com/apppasswords и обновите на Vercel.

---

## 🎉 После настройки:

✅ Email верификация работает на localhost  
✅ Email верификация работает на Vercel (production)  
✅ Коды приходят пользователям на любые email адреса  
✅ Все безопасно и зашифровано  

**Готово к использованию в продакшене!** 🚀
