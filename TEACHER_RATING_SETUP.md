# Настройка системы рейтинга преподавателей

## 🎉 Реализованный функционал

Система рейтинга преподавателей полностью реализована и включает:

### ✅ Что было сделано:

1. **База данных (Prisma)**
   - Модель `Teacher` - профили преподавателей
   - Модель `TeacherRating` - оценки по 4 категориям (знание предмета, качество преподавания, коммуникабельность, справедливость)
   - Модель `TeacherReview` - текстовые отзывы с модерацией
   - Модель `TeacherReviewHelpful` - оценка полезности отзывов
   - Модель `TeacherSubject` - связь преподавателей с предметами

2. **API Endpoints**
   - `GET/POST /api/teachers` - список и создание преподавателей
   - `GET/PATCH/DELETE /api/teachers/[id]` - просмотр, редактирование, удаление
   - `GET/POST /api/teachers/[id]/rating` - оценка преподавателя
   - `GET/POST /api/teachers/[id]/reviews` - отзывы о преподавателе
   - `PATCH/DELETE /api/teachers/reviews/[reviewId]` - модерация отзывов
   - `POST /api/teachers/reviews/[reviewId]/helpful` - оценка полезности отзыва
   - `POST /api/teachers/[id]/photo` - загрузка фото через Supabase

3. **Страницы пользователя**
   - `/teachers` - список всех преподавателей с фильтрами и поиском
   - `/teachers/[id]` - профиль преподавателя с детальным рейтингом и отзывами

4. **Админ-панель**
   - `/admin/teachers` - управление преподавателями (CRUD)
   - Добавление/редактирование профилей
   - Загрузка фотографий
   - Привязка к предметам

5. **Компоненты**
   - `TeacherCard` - карточка преподавателя
   - `TeacherRatingForm` - форма оценки по категориям
   - `TeacherReviewForm` - форма написания отзыва
   - `TeacherReviewItem` - отображение отзыва с оценкой полезности
   - `TeacherProfileClient` - полная страница профиля
   - `AdminTeachersClient` - админ-панель управления

6. **Навигация**
   - Добавлена ссылка "Преподаватели" в главное меню (desktop + mobile)
   - Добавлена карточка "Преподаватели" в админ-панели

## 📋 Шаги для запуска

### 1. Применить миграцию базы данных

```bash
cd secka
npx prisma migrate dev --name add_teacher_rating_system
```

Это создаст все необходимые таблицы в вашей базе данных Neon.

### 2. Настроить Supabase Storage для фото (опционально)

Если у вас еще нет бакета для фотографий преподавателей:

1. Откройте ваш проект в [Supabase Dashboard](https://app.supabase.com)
2. Перейдите в раздел **Storage**
3. Создайте новый bucket с именем `teachers`
4. Сделайте bucket **публичным** (Public bucket)
5. Настройте политики доступа:

```sql
-- Политика для чтения (все могут просматривать)
CREATE POLICY "Public Access"
ON storage.objects FOR SELECT
USING ( bucket_id = 'teachers' );

-- Политика для загрузки (только аутентифицированные)
CREATE POLICY "Authenticated users can upload"
ON storage.objects FOR INSERT
WITH CHECK (
  bucket_id = 'teachers' 
  AND auth.role() = 'authenticated'
);

-- Политика для удаления (только аутентифицированные)
CREATE POLICY "Authenticated users can delete"
ON storage.objects FOR DELETE
USING (
  bucket_id = 'teachers'
  AND auth.role() = 'authenticated'
);
```

### 3. Проверить переменные окружения

Убедитесь, что в `.env.local` есть:

```env
# Database
DATABASE_URL="your_neon_database_url"

# NextAuth
NEXTAUTH_SECRET="your_secret"
NEXTAUTH_URL="http://localhost:3000"

# Supabase (для загрузки фото)
NEXT_PUBLIC_SUPABASE_URL="your_supabase_url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your_supabase_anon_key"
SUPABASE_SERVICE_ROLE_KEY="your_supabase_service_role_key"
```

### 4. Запустить проект

```bash
npm run dev
```

Откройте http://localhost:3000

## 🎯 Как использовать

### Для студентов:

1. **Просмотр преподавателей**
   - Перейдите в раздел "Преподаватели" в главном меню
   - Используйте фильтры по кафедре и поиск
   - Нажмите на карточку преподавателя для просмотра профиля

2. **Оценка преподавателя**
   - На странице профиля преподавателя нажмите "Оценить"
   - Поставьте оценки по 4 категориям (1-5 звезд)
   - Сохраните оценку

3. **Написание отзыва**
   - На странице профиля нажмите "Написать отзыв"
   - Напишите свое мнение (минимум 10 символов)
   - Можно оставить анонимно
   - Отзыв будет опубликован после модерации

4. **Оценка полезности отзывов**
   - Под каждым отзывом есть кнопки 👍 и 👎
   - Отметьте полезные/бесполезные отзывы

### Для админов/модераторов:

1. **Добавление преподавателя**
   - Перейдите в `/admin/teachers`
   - Нажмите "Добавить преподавателя"
   - Заполните ФИО, кафедру, должность и т.д.
   - Загрузите фото (макс 2MB)
   - Выберите предметы, которые преподает
   - Сохраните

2. **Редактирование**
   - В списке преподавателей нажмите иконку редактирования
   - Измените нужные данные
   - Сохраните изменения

3. **Модерация отзывов**
   - Зайдите на страницу преподавателя
   - Увидите все отзывы (включая ожидающие модерации)
   - Одобрите или удалите отзывы

4. **Удаление преподавателя**
   - В списке нажмите иконку удаления
   - Подтвердите действие

## 🔧 Технические детали

### Структура рейтинга

Каждый преподаватель оценивается по 4 категориям:
- **Знание предмета** (1-5 звезд)
- **Качество преподавания** (1-5 звезд)
- **Коммуникабельность** (1-5 звезд)
- **Справедливость в оценках** (1-5 звезд)

Общий рейтинг = среднее арифметическое всех категорий

### Модерация отзывов

- Все отзывы по умолчанию имеют статус `PENDING`
- Только админы/модераторы могут одобрять отзывы (статус `APPROVED`)
- Обычные пользователи видят только одобренные отзывы
- Админы видят все отзывы

### Загрузка фото

- Фото хранятся в Supabase Storage (bucket `teachers`)
- Максимальный размер файла: 2MB
- Поддерживаемые форматы: все изображения (image/*)
- Старые фото автоматически удаляются при загрузке новых

### Безопасность

- Создавать/редактировать преподавателей могут только `ADMIN` и `MODERATOR`
- Удалять преподавателей может только `ADMIN`
- Оценивать и писать отзывы могут все авторизованные студенты
- Один студент может оставить только одну оценку и один отзыв на преподавателя

## 📊 База данных

### Новые таблицы:

- `teachers` - профили преподавателей
- `teacher_subjects` - связь преподавателей с предметами (many-to-many)
- `teacher_ratings` - оценки преподавателей
- `teacher_reviews` - отзывы о преподавателях
- `teacher_review_helpful` - оценки полезности отзывов

### Индексы для производительности:

- По кафедре (`department`)
- По среднему рейтингу (`averageRating`)
- По статусу активности (`isActive`)
- По статусу отзыва (`status`)
- И другие для оптимизации запросов

## 🚀 Деплой на Vercel

1. **Обновите package.json** (если нужно):

```json
{
  "scripts": {
    "build": "prisma generate && next build"
  }
}
```

2. **Настройте переменные окружения в Vercel**:
   - Добавьте все переменные из `.env.local`
   - Особенно важны `DATABASE_URL` и Supabase ключи

3. **Push в GitHub и деплой**:

```bash
git add .
git commit -m "Add teacher rating system"
git push
```

Vercel автоматически задеплоит изменения.

## ✅ Чек-лист перед запуском

- [ ] Применена миграция базы данных
- [ ] Создан Supabase bucket `teachers`
- [ ] Настроены политики доступа в Supabase
- [ ] Проверены переменные окружения
- [ ] Проект запущен локально
- [ ] Создан тестовый преподаватель
- [ ] Протестирована оценка и отзывы
- [ ] Проверена админ-панель

## 🎓 Готово к использованию!

Система полностью функциональна и готова к использованию. Студенты могут оценивать преподавателей, а администраторы управлять профилями.

---

**Дата создания:** 2026-02-05
**Автор:** Rovo Dev (AI Assistant)
