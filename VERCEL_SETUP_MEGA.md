# 🚀 Настройка MEGA аккаунта на Vercel

## ✅ Локальная настройка выполнена!

MEGA аккаунт уже настроен локально в `.env.local` и протестирован:
- ✅ Email: `sechenovplus@gmail.com`
- ✅ Подключение успешно
- ✅ Готово к использованию

## 📋 Настройка на Vercel (2 простых шага)

### Шаг 1: Добавьте переменные окружения на Vercel

1. **Откройте ваш проект на Vercel:**
   - Перейдите на https://vercel.com/dashboard
   - Выберите проект `sechenov-plus`

2. **Перейдите в Settings → Environment Variables**

3. **Добавьте следующие переменные:**

```
Имя: MEGA_EMAIL_1
Значение: sechenovplus@gmail.com
Environment: Production, Preview, Development
```

```
Имя: MEGA_PASSWORD_1
Значение: Nebovzvezdaxvse.23
Environment: Production, Preview, Development
```

```
Имя: MEGA_FOLDER_NAME_1
Значение: sechenov-plus-materials
Environment: Production, Preview, Development
```

4. **Нажмите "Save"** для каждой переменной

### Шаг 2: Примените миграцию базы данных на Neon

1. **Откройте Neon Console:**
   - Перейдите на https://console.neon.tech/
   - Выберите ваш проект

2. **Откройте SQL Editor**

3. **Выполните следующий SQL:**

```sql
-- Добавить поле megaAccountId для отслеживания MEGA аккаунтов
ALTER TABLE "materials" ADD COLUMN IF NOT EXISTS "megaAccountId" INTEGER;

-- Проверить что поле добавлено
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'materials' AND column_name = 'megaAccountId';
```

4. **Результат должен показать:**
```
column_name    | data_type
---------------+-----------
megaAccountId  | integer
```

### Шаг 3: Redeploy на Vercel

1. **Вернитесь в Vercel Dashboard**
2. **Deployments → Latest Deployment**
3. **Нажмите три точки (⋯) → Redeploy**
4. **Выберите "Redeploy"** для применения новых переменных окружения

## ✅ Готово! Теперь проверьте работу

### Тест 1: Загрузка файла администратором

1. Войдите как администратор
2. Перейдите на `/materials/upload`
3. Вы должны увидеть:
   - ✨ "Файл (PDF или DOCX, до **500MB**)" (вместо 10MB)
   - 🔵 Голубой badge "Админ: увеличенный лимит"
   - 📦 Информация о MEGA Storage в голубом блоке

4. Загрузите файл >10MB (например, 15MB)
5. Файл должен успешно загрузиться в MEGA

### Тест 2: Проверка в базе данных

Выполните в Neon SQL Editor:

```sql
-- Проверить файлы в MEGA
SELECT 
  title,
  fileName,
  fileSize / 1024 / 1024 as size_mb,
  storageType,
  megaAccountId,
  externalUrl
FROM materials
WHERE storageType = 'EXTERNAL_MEGA'
ORDER BY createdAt DESC
LIMIT 5;
```

Вы должны увидеть:
- `storageType` = `EXTERNAL_MEGA`
- `megaAccountId` = `1` (для первого аккаунта)
- `externalUrl` = ссылка на MEGA файл

### Тест 3: Скачивание файла

1. Найдите загруженный файл в списке материалов
2. Нажмите "Скачать"
3. Файл должен открыться/скачаться из MEGA прозрачно для пользователя

## 🎯 Что дальше?

### Добавить больше MEGA аккаунтов (опционально)

Если нужно больше места:

1. **Создайте второй MEGA аккаунт:**
   - Например: `sechenovplus2@gmail.com`
   - Или используйте Gmail alias: `sechenovplus+2@gmail.com`

2. **Добавьте на Vercel:**
   ```
   MEGA_EMAIL_2 = sechenovplus2@gmail.com
   MEGA_PASSWORD_2 = YourPassword2
   MEGA_FOLDER_NAME_2 = sechenov-plus-materials
   ```

3. **Redeploy**

Система автоматически распределит файлы между аккаунтами:
- Файл 1 → Аккаунт 1 (20GB)
- Файл 2 → Аккаунт 2 (20GB)
- Файл 3 → Аккаунт 1 (rotation)
- И так далее...

### Хранилище растёт линейно:
- 1 аккаунт = **20GB**
- 2 аккаунта = **40GB**
- 3 аккаунта = **60GB**
- N аккаунтов = **N × 20GB**

## 📊 Мониторинг использования

### Проверить распределение файлов по аккаунтам:

```sql
SELECT 
  megaAccountId as "Аккаунт",
  COUNT(*) as "Количество файлов",
  ROUND(SUM(fileSize) / 1024.0 / 1024.0 / 1024.0, 2) as "Размер (GB)"
FROM materials
WHERE storageType = 'EXTERNAL_MEGA'
GROUP BY megaAccountId
ORDER BY megaAccountId;
```

### Проверить общее использование MEGA:

```sql
SELECT 
  COUNT(*) as total_files,
  ROUND(SUM(fileSize) / 1024.0 / 1024.0 / 1024.0, 2) as total_size_gb
FROM materials
WHERE storageType = 'EXTERNAL_MEGA';
```

## ⚠️ Важные замечания

1. **Первый аккаунт настроен:** `sechenovplus@gmail.com`
2. **Folder будет создана автоматически** при первой загрузке
3. **Бесплатный MEGA:** 20GB хранилища + трафик ~5GB/день
4. **Админы могут загружать до 500MB** на файл
5. **Обычные пользователи ограничены 10MB**

## 🆘 Troubleshooting

### Проблема: "External storage is not configured"
**Решение:** Проверьте что `MEGA_EMAIL_1` и `MEGA_PASSWORD_1` добавлены на Vercel и сделайте Redeploy

### Проблема: "MEGA authentication failed"
**Решение:** Проверьте правильность email/пароля в Vercel Environment Variables

### Проблема: Файл не загружается
**Решение:** 
- Проверьте логи Vercel (Runtime Logs)
- Убедитесь что файл <500MB для админов или <10MB для пользователей

## 📖 Дополнительная документация

Подробная информация о множественных аккаунтах:
- `MEGA_MULTIPLE_ACCOUNTS_GUIDE.md`

---

**🎉 Готово! Ваша система готова работать с MEGA хранилищем!**
