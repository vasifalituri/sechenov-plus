# 🔄 Инструкция: Обновление предметов в продакшене

## Проблема
Локальный HTML файл не может отправить cookies, поэтому API не видит вашу сессию.

## ✅ Решение: Вызов через консоль браузера

### Шаг 1: Откройте сайт

Откройте в браузере: https://sechenov-plus.vercel.app

### Шаг 2: Войдите как админ

Убедитесь что вы вошли как администратор.

### Шаг 3: Откройте консоль разработчика

Нажмите **F12** или **Ctrl+Shift+I** (Windows/Linux) или **Cmd+Option+I** (Mac)

### Шаг 4: Вставьте и выполните код

Перейдите во вкладку **Console** и вставьте этот код:

```javascript
// Update subjects in production
fetch('/api/admin/update-subjects', {
  method: 'POST',
  credentials: 'include'
})
.then(response => response.json())
.then(data => {
  console.log('✅ Success!');
  console.log('Summary:', data.summary);
  console.log('Created:', data.summary.created);
  console.log('Updated:', data.summary.updated);
  console.log('Deleted:', data.summary.deleted);
  console.log('\nFull response:', data);
  
  alert('✅ Subjects updated successfully!\n\n' +
        'Created: ' + data.summary.created + '\n' +
        'Updated: ' + data.summary.updated + '\n' +
        'Deleted: ' + data.summary.deleted);
})
.catch(error => {
  console.error('❌ Error:', error);
  alert('❌ Error: ' + error.message);
});
```

### Шаг 5: Нажмите Enter

Подождите несколько секунд, и вы увидите результат в консоли.

---

## ✅ Ожидаемый результат:

В консоли должно появиться:
```
✅ Success!
Summary: {created: 42, updated: 8, deleted: 10}
Created: 42
Updated: 8
Deleted: 10
```

И всплывающее окно (alert) с подтверждением.

---

## 🧪 Проверка результата

После успешного обновления проверьте:

1. Откройте: https://sechenov-plus.vercel.app/materials/upload
2. Кликните на поле "Предмет"
3. Должны увидеть:
   - **Первым:** "Другое"
   - **Далее:** все 50 предметов в алфавитном порядке

---

## ⚠️ Если возникла ошибка

### Ошибка: "Unauthorized"
- **Причина:** Не вошли как админ
- **Решение:** Войдите на сайт под админ аккаунтом

### Ошибка: "Failed to fetch"
- **Причина:** Проблемы с сетью или CORS
- **Решение:** 
  1. Обновите страницу (F5)
  2. Убедитесь что вы на https://sechenov-plus.vercel.app
  3. Попробуйте ещё раз

### Ошибка: "Route not found"
- **Причина:** Деплой не завершился
- **Решение:** Подождите ещё 1-2 минуты и попробуйте снова

---

## 🗑️ После успешного обновления

**Обязательно удалите временный endpoint!**

Это нужно для безопасности. Выполните:

```bash
# Удалить временный endpoint
git rm src/app/api/admin/update-subjects/route.ts
git commit -m "chore: remove temporary update-subjects endpoint"
git push
```

Или я могу это сделать за вас после подтверждения.

---

## 📊 Альтернативный способ (через Postman/Insomnia)

Если консоль браузера не работает:

1. Откройте Postman или Insomnia
2. Создайте POST запрос: `https://sechenov-plus.vercel.app/api/admin/update-subjects`
3. В Headers добавьте Cookie из браузера:
   - Откройте DevTools → Application → Cookies
   - Скопируйте значение cookie `next-auth.session-token`
   - Добавьте в Postman: `Cookie: next-auth.session-token=значение`
4. Отправьте запрос

---

**Текущий статус**: ⏳ Ожидание выполнения через консоль браузера
