# Подробное руководство по установке

Это пошаговое руководство поможет вам настроить платформу Sechenov+ на вашем компьютере.

## Требования к системе

### Минимальные требования
- **ОС:** Windows 10+, macOS 10.15+, или Linux
- **Процессор:** 2 ядра
- **RAM:** 4GB
- **Место на диске:** 500MB

### Программное обеспечение

1. **Node.js** (версия 18 или выше)
   - Скачайте с https://nodejs.org/
   - Проверьте установку: `node --version`

2. **PostgreSQL** (версия 14 или выше)
   - Windows: https://www.postgresql.org/download/windows/
   - macOS: `brew install postgresql`
   - Linux: `sudo apt install postgresql`
   - Проверьте установку: `psql --version`

3. **Git** (опционально)
   - Скачайте с https://git-scm.com/

## Пошаговая установка

### Шаг 1: Подготовка файлов проекта

Если у вас есть архив проекта:
```bash
# Распакуйте архив в нужную папку
cd путь/к/проекту
```

### Шаг 2: Установка зависимостей

```bash
# В корневой папке проекта выполните:
npm install

# Дождитесь завершения (может занять 2-5 минут)
```

### Шаг 3: Настройка PostgreSQL

#### Windows

1. Откройте pgAdmin или командную строку PostgreSQL
2. Создайте базу данных:
```sql
CREATE DATABASE medical_exam_platform;
```

3. Запомните:
   - Username (обычно: `postgres`)
   - Password (тот, который вы установили)
   - Port (обычно: `5432`)

#### macOS/Linux

```bash
# Запустите PostgreSQL
brew services start postgresql  # macOS
sudo service postgresql start   # Linux

# Войдите в psql
psql postgres

# Создайте базу данных
CREATE DATABASE medical_exam_platform;

# Выйдите
\q
```

### Шаг 4: Создание файла конфигурации

1. Скопируйте файл `.env.example`:
```bash
# Windows (PowerShell)
Copy-Item .env.example .env.local

# macOS/Linux
cp .env.example .env.local
```

2. Откройте `.env.local` в текстовом редакторе

3. Обновите `DATABASE_URL`:
```env
# Формат: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/medical_exam_platform?schema=public"
```

4. Создайте секретный ключ для `NEXTAUTH_SECRET`:
```bash
# Сгенерируйте случайную строку
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

Скопируйте результат и вставьте в `.env.local`:
```env
NEXTAUTH_SECRET="ваш_сгенерированный_ключ"
```

### Шаг 5: Инициализация базы данных

```bash
# 1. Создать структуру таблиц
npx prisma migrate dev --name init

# Вы увидите: "✔ Generated Prisma Client"

# 2. Заполнить начальными данными
npx prisma db seed

# Вы увидите:
# ✅ Created admin user: admin@medexam.az
# ✅ Created 18 subjects
# 🎉 Seeding completed!
```

### Шаг 6: Проверка установки

```bash
# Откройте Prisma Studio для просмотра БД
npx prisma studio

# Откроется браузер с адресом http://localhost:5555
# Проверьте, что есть:
# - 1 пользователь (admin)
# - 18 предметов
```

### Шаг 7: Запуск приложения

```bash
# Запустите в режиме разработки
npm run dev

# Вы увидите:
# ▲ Next.js 14.1.0
# - Local:   http://localhost:3000
# ✓ Ready in 2s
```

### Шаг 8: Первый вход

1. Откройте браузер: http://localhost:3000
2. Войдите как администратор:
   - **Email:** admin@medexam.az
   - **Пароль:** admin123
3. **Сразу смените пароль!**

## Проверка работоспособности

### Тест 1: Регистрация пользователя

1. Выйдите из админ аккаунта
2. Перейдите на `/register`
3. Зарегистрируйте тестового пользователя
4. Войдите как админ
5. Перейдите в `/admin/users`
6. Одобрите нового пользователя
7. Войдите как новый пользователь

### Тест 2: Загрузка материала

1. Войдите как обычный пользователь
2. Перейдите в `/materials/upload`
3. Загрузите тестовый PDF файл
4. Войдите как админ
5. Перейдите в `/admin/materials`
6. Одобрите материал
7. Проверьте, что он виден в `/materials`

### Тест 3: Создание обсуждения

1. Войдите как обычный пользователь
2. Перейдите в `/discussions/new`
3. Создайте тестовую тему
4. Войдите как админ
5. Одобрите обсуждение
6. Проверьте, что оно видно в `/discussions`
7. Добавьте комментарий

## Решение проблем

### Ошибка: "Cannot connect to database"

**Причина:** PostgreSQL не запущен или неверные данные подключения

**Решение:**
```bash
# Проверьте статус PostgreSQL
# Windows: откройте Services и найдите PostgreSQL
# macOS:
brew services list
# Linux:
sudo service postgresql status

# Проверьте DATABASE_URL в .env.local
# Убедитесь, что username, password и port правильные
```

### Ошибка: "Prisma Client is not generated"

**Решение:**
```bash
npx prisma generate
```

### Ошибка: "Port 3000 is already in use"

**Решение:**
```bash
# Используйте другой порт
npm run dev -- -p 3001

# Или остановите процесс на порту 3000
# Windows:
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# macOS/Linux:
lsof -ti:3000 | xargs kill
```

### Ошибка: "Cannot find module"

**Решение:**
```bash
# Удалите node_modules и переустановите
rm -rf node_modules
npm install
```

### Ошибка загрузки файлов

**Решение:**
```bash
# Создайте папку uploads вручную
mkdir -p public/uploads
```

### База данных не обновляется

**Решение:**
```bash
# Сбросьте БД и начните заново
npx prisma migrate reset
npx prisma db seed
```

## Продакшн деплой

### Подготовка к деплою

1. **Сборка проекта:**
```bash
npm run build
```

2. **Тест продакшн версии:**
```bash
npm start
```

### Рекомендуемые платформы

1. **Vercel** (рекомендуется для Next.js)
   - Бесплатный tier
   - Автоматический деплой
   - Инструкция: https://vercel.com/docs

2. **Railway**
   - PostgreSQL включен
   - Простой деплой
   - Инструкция: https://railway.app/

3. **DigitalOcean App Platform**
   - Полный контроль
   - Масштабируемость

### Переменные окружения для продакшена

Не забудьте установить на хостинге:
```env
DATABASE_URL="ваш_продакшн_url"
NEXTAUTH_URL="https://ваш-домен.com"
NEXTAUTH_SECRET="безопасный_секрет"
```

## Бэкап и восстановление

### Создание бэкапа

```bash
# Бэкап базы данных
pg_dump -U postgres medical_exam_platform > backup.sql

# Бэкап файлов
tar -czf uploads-backup.tar.gz public/uploads/
```

### Восстановление

```bash
# Восстановление БД
psql -U postgres medical_exam_platform < backup.sql

# Восстановление файлов
tar -xzf uploads-backup.tar.gz
```

## Дополнительные команды

```bash
# Просмотр логов Prisma
npx prisma studio

# Форматирование кода
npm run lint

# Очистка кеша Next.js
rm -rf .next

# Обновление зависимостей
npm update
```

## Получение помощи

Если проблема не решена:

1. Проверьте логи в терминале
2. Откройте консоль браузера (F12)
3. Проверьте файл `.env.local`
4. Убедитесь, что PostgreSQL работает
5. Перезапустите приложение

---

**Готово!** Платформа установлена и готова к использованию 🎉
