# 🚀 Деплой на Vercel - Пошаговая инструкция

## Предварительные требования

✅ У вас есть аккаунт на [Vercel](https://vercel.com)
✅ Проект загружен на GitHub
✅ База данных Neon настроена
✅ Supabase bucket `teachers` создан

---

## Шаг 1: Подготовка репозитория

### 1.1 Убедитесь, что .gitignore правильно настроен

Файл `.gitignore` должен содержать:

```
# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
prisma/migrations/**/migration.sql
```

### 1.2 Закоммитьте изменения

```bash
git add .
git commit -m "Add teacher rating system"
git push origin main
```

---

## Шаг 2: Создание проекта на Vercel

### 2.1 Импорт проекта

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите **"Add New..."** → **"Project"**
3. Выберите репозиторий GitHub (`vasifalituri/sechenov-plus`)
4. Нажмите **"Import"**

### 2.2 Настройка проекта

**Framework Preset:** Next.js (автоматически определится)
**Root Directory:** `secka` ⚠️ ВАЖНО!
**Build Command:** `npm run build` (уже настроено в package.json)
**Output Directory:** `.next` (по умолчанию)
**Install Command:** `npm install`

---

## Шаг 3: Настройка переменных окружения

В разделе **Environment Variables** добавьте следующие переменные:

### 3.1 База данных (Neon)

| Name | Value |
|------|-------|
| `DATABASE_URL` | `postgresql://user:password@host/database?sslmode=require` |

**Где взять:**
- Откройте [Neon Dashboard](https://console.neon.tech)
- Выберите проект → **Connection string**
- Скопируйте **Connection string**

### 3.2 NextAuth

| Name | Value |
|------|-------|
| `NEXTAUTH_SECRET` | `ваш-секретный-ключ` |
| `NEXTAUTH_URL` | `https://your-app.vercel.app` |

**Генерация NEXTAUTH_SECRET:**
```bash
openssl rand -base64 32
```

⚠️ **ВАЖНО:** `NEXTAUTH_URL` будет известен после первого деплоя. Сначала оставьте пустым, затем обновите.

### 3.3 Supabase

| Name | Value |
|------|-------|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://your-project.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `your-anon-key` |
| `SUPABASE_SERVICE_ROLE_KEY` | `your-service-role-key` |

**Где взять:**
- Откройте [Supabase Dashboard](https://app.supabase.com)
- Проект → **Settings** → **API**
- Скопируйте ключи

### 3.4 Email (опционально)

Если настроена отправка email:

| Name | Value |
|------|-------|
| `EMAIL_SERVER_HOST` | `smtp.gmail.com` |
| `EMAIL_SERVER_PORT` | `587` |
| `EMAIL_SERVER_USER` | `your-email@gmail.com` |
| `EMAIL_SERVER_PASSWORD` | `your-app-password` |
| `EMAIL_FROM` | `noreply@sechenov.plus` |

---

## Шаг 4: Первый деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения билда (2-5 минут)
3. После успешного деплоя скопируйте URL (например: `https://sechenov-plus.vercel.app`)

---

## Шаг 5: Обновление NEXTAUTH_URL

1. В Vercel Dashboard → **Settings** → **Environment Variables**
2. Найдите `NEXTAUTH_URL`
3. Обновите значение на ваш реальный URL: `https://your-app.vercel.app`
4. Нажмите **"Redeploy"** → **"Use existing Build Cache"**

---

## Шаг 6: Настройка базы данных

### 6.1 Проверка миграций

Миграции должны применяться автоматически при билде (настроено в `package.json`).

Если нужно применить вручную:

```bash
# Локально с production DATABASE_URL
DATABASE_URL="your-production-url" npx prisma migrate deploy
```

### 6.2 Заполнение тестовыми данными

```bash
# Только для первого раза!
DATABASE_URL="your-production-url" npm run prisma:seed
```

---

## Шаг 7: Проверка работы

1. Откройте ваш сайт: `https://your-app.vercel.app`
2. Войдите как админ:
   - Email: `admin@sechenov.plus`
   - Password: `admin123`
3. Проверьте все разделы:
   - ✅ `/teachers` - список преподавателей
   - ✅ `/admin/teachers` - админ-панель
   - ✅ Загрузка фото преподавателя

---

## Шаг 8: Настройка домена (опционально)

1. В Vercel Dashboard → **Settings** → **Domains**
2. Добавьте ваш домен (например: `sechenov.plus`)
3. Настройте DNS записи согласно инструкциям Vercel
4. Обновите `NEXTAUTH_URL` на новый домен

---

## 🔧 Troubleshooting

### Ошибка: "Failed to connect to database"

**Решение:**
1. Проверьте `DATABASE_URL` в переменных окружения
2. Убедитесь, что в URL есть `?sslmode=require`
3. Проверьте, что IP Vercel не заблокирован в Neon

### Ошибка: "Prisma Client not generated"

**Решение:**
- Добавлено в `package.json`: `"postinstall": "prisma generate"`
- Vercel автоматически выполнит эту команду

### Ошибка: "NEXTAUTH_SECRET is not set"

**Решение:**
- Добавьте переменную `NEXTAUTH_SECRET` в Vercel
- Сгенерируйте новый ключ: `openssl rand -base64 32`

### Фото не загружаются в Supabase

**Решение:**
1. Проверьте, что bucket `teachers` создан
2. Проверьте, что bucket публичный
3. Проверьте политики доступа (см. SUPABASE_SETUP.md)
4. Проверьте переменные Supabase в Vercel

---

## 📊 Мониторинг

### Логи

Vercel → **Deployments** → выберите деплой → **Logs**

### Функции (Serverless)

Vercel → **Functions** - просмотр всех API routes

### Аналитика

Vercel → **Analytics** - статистика посещений

---

## 🔄 Автоматический деплой

После настройки каждый `git push` в main ветку будет автоматически деплоиться на Vercel!

```bash
git add .
git commit -m "Update feature"
git push origin main
```

Vercel автоматически:
1. Запустит билд
2. Применит миграции
3. Задеплоит новую версию

---

## ✅ Чек-лист успешного деплоя

- [ ] Проект импортирован в Vercel
- [ ] Root Directory установлен на `secka`
- [ ] Все переменные окружения добавлены
- [ ] Первый деплой успешен
- [ ] `NEXTAUTH_URL` обновлен
- [ ] База данных работает
- [ ] Тестовые данные загружены
- [ ] Авторизация работает
- [ ] Раздел преподавателей открывается
- [ ] Загрузка фото работает
- [ ] Оценки и отзывы работают

---

## 🎉 Готово!

Ваш проект успешно задеплоен на Vercel!

**Полезные ссылки:**
- 🌐 Production: `https://your-app.vercel.app`
- 📊 Dashboard: `https://vercel.com/dashboard`
- 📖 Docs: `https://vercel.com/docs`

---

## 🔐 Безопасность

После деплоя:

1. **Смените пароль админа:**
   - Войдите как `admin@sechenov.plus`
   - Перейдите в профиль
   - Измените пароль с `admin123` на надежный

2. **Настройте CORS** (если нужно):
   - `next.config.js` уже настроен

3. **Мониторинг:**
   - Включите уведомления о деплое в Vercel
   - Настройте alerts в Neon Dashboard

---

**Готово! Проект работает в production! 🚀**
