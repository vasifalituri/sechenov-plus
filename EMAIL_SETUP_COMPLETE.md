# ✅ Email верификация настроена на Gmail SMTP

## 🎯 Что было сделано:

✅ Установлен пакет `nodemailer` для отправки email  
✅ Создан сервис `src/lib/email.ts` с Gmail SMTP  
✅ Обновлен endpoint регистрации `/api/auth/register`  
✅ Обновлен endpoint отправки кода `/api/auth/send-verification-code`  
✅ Добавлены переменные окружения в `.env.local`  
✅ Создан тестовый endpoint `/api/test-email`  
✅ Создана подробная документация  

---

## 🚀 Следующие шаги (ОБЯЗАТЕЛЬНО):

### 1️⃣ Получите App Password от Google

**Важно:** Это займет 2 минуты, но необходимо для работы!

1. Откройте: **https://myaccount.google.com/apppasswords**
2. Войдите в свой Google аккаунт (vasifalituri@gmail.com)
3. **Если страница не открывается:**
   - Сначала включите 2FA: https://myaccount.google.com/security
   - Найдите "2-Step Verification" → Enable
   - После этого снова откройте страницу App passwords
4. Введите имя: `Sechenov Plus`
5. Нажмите **Create**
6. **СКОПИРУЙТЕ** 16-значный пароль (будет выглядеть так: `abcd efgh ijkl mnop`)

### 2️⃣ Добавьте пароль в .env.local

Откройте файл: `secka/.env.local`

Найдите строку:
```env
GMAIL_APP_PASSWORD="your-app-password-here"
```

Замените на (БЕЗ пробелов в пароле):
```env
GMAIL_APP_PASSWORD="abcdefghijklmnop"
```

Пример:
```env
GMAIL_USER="vasifalituri@gmail.com"
GMAIL_APP_PASSWORD="abcdefghijklmnop"  ← ваш реальный пароль
```

### 3️⃣ Перезапустите сервер

```bash
cd secka
npm run dev
```

### 4️⃣ Протестируйте

Откройте в браузере:
```
http://localhost:3000/api/test-email
```

Должно показать:
```json
{
  "success": true,
  "message": "Gmail SMTP is configured correctly!",
  "config": {
    "user": "vasifalituri@gmail.com",
    "passwordSet": true
  }
}
```

Отправьте тестовое письмо:
```
http://localhost:3000/api/test-email?email=vasifalituri@gmail.com
```

Проверьте ваш email (и папку "Спам")!

---

## 📧 Как будет работать:

1. **Пользователь регистрируется** → код отправляется на его email
2. **Письмо приходит от:** `Sechenov+ <vasifalituri@gmail.com>`
3. **Красивый HTML дизайн** с кодом подтверждения
4. **Код действителен** 15 минут

---

## 🔧 Файлы которые были изменены:

```
secka/
├── package.json                                    ✅ Добавлен nodemailer
├── .env.local                                      ✅ Добавлены GMAIL_* переменные
├── .env.example                                    ✅ Обновлен пример
├── src/
│   ├── lib/
│   │   └── email.ts                               ✅ НОВЫЙ - Gmail SMTP сервис
│   └── app/
│       └── api/
│           ├── auth/
│           │   ├── register/route.ts              ✅ Использует Gmail
│           │   └── send-verification-code/route.ts ✅ Использует Gmail
│           └── test-email/route.ts                ✅ НОВЫЙ - Тестирование
├── GMAIL_SMTP_SETUP.md                            ✅ НОВЫЙ - Полная инструкция
├── QUICK_SETUP_GMAIL.md                           ✅ НОВЫЙ - Быстрый старт
└── EMAIL_SETUP_COMPLETE.md                        ✅ НОВЫЙ - Этот файл
```

---

## ❓ Частые проблемы:

### Ошибка: "Invalid login: 535"
→ Неверный App Password. Создайте новый и вставьте БЕЗ пробелов.

### Ошибка: "Missing credentials"
→ Не установлены GMAIL_USER или GMAIL_APP_PASSWORD в .env.local

### Письма не приходят
→ Проверьте папку "Спам"
→ Проверьте логи сервера в терминале

### Страница App Passwords не открывается
→ Сначала включите 2FA в Google аккаунте

---

## 📚 Документация:

- **Быстрый старт (2 мин)**: `QUICK_SETUP_GMAIL.md`
- **Полная инструкция**: `GMAIL_SMTP_SETUP.md`
- **Пример env**: `.env.example`

---

## 🎉 Готово!

После настройки App Password, ваша email верификация будет работать на **100%** без покупки домена!

**Лимиты:** 500 писем/день (более чем достаточно для MVP)

**Следующий шаг:** Когда будете готовы к продакшену, можете купить домен за $1-10/год и переключиться на Resend для более профессионального вида.
