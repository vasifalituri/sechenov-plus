# 📧 Улучшения системы подтверждения email

## Проблемы которые были решены:

### 1. ❌ Админ мог одобрить пользователя без подтверждения email
**Решение:**
- Добавлена проверка `emailVerified` перед одобрением
- Кнопка "Одобрить" теперь **неактивна** для пользователей без подтверждённого email
- Показывается tooltip: "Пользователь должен подтвердить email"
- API возвращает ошибку при попытке одобрить без подтверждения

### 2. ❌ Не было видно статус подтверждения email в админ-панели
**Решение:**
- Добавлен badge рядом с именем пользователя:
  - 🟢 "Email подтвержден" (зеленый)
  - 🟡 "Email не подтвержден" (желтый)
- API теперь возвращает поле `emailVerified` в списке пользователей

### 3. ❌ Плохая обработка ошибок отправки email
**Решение:**
- Детальное логирование ошибок Resend для отладки
- Специфичные сообщения об ошибках для пользователя:
  - Проблемы с доменом
  - Rate limiting
  - Неверный формат email
- В dev режиме показывается debug информация

### 4. ❌ Можно было спамить кнопку "Отправить повторно"
**Решение:**
- Добавлен таймер 60 секунд после отправки
- Кнопка показывает "Повторная отправка через Xs"
- Предотвращает злоупотребление и rate limiting от Resend

---

## 🎨 Изменения в UI

### Админ-панель пользователей (`/admin/users`)

**До:**
```
Иван Иванов
student@example.com • 3 курс • STUDENT
[Одобрить] [Отклонить]
```

**После:**
```
Иван Иванов  [🟡 Email не подтвержден]
student@example.com • 3 курс • STUDENT
[Одобрить (disabled)] [Отклонить]
```

### Форма подтверждения email

**До:**
- Кнопка "Отправить повторно" всегда активна

**После:**
- "Отправить повторно" → "Повторная отправка через 60с" → ... → "Отправить повторно"
- Анимация спиннера при отправке
- Показывается конкретная ошибка если что-то не так

---

## 🔧 Изменения в коде

### 1. API: `/api/admin/users` (PATCH)

```typescript
// Проверка email перед одобрением
if (status === 'APPROVED') {
  const user = await prisma.user.findUnique({
    where: { id: userId },
    select: { emailVerified: true, email: true },
  });

  if (!user?.emailVerified) {
    return NextResponse.json({
      success: false,
      error: 'Невозможно одобрить пользователя. Email не подтвержден.',
      details: 'Пользователь должен подтвердить свой email перед одобрением.'
    }, { status: 400 });
  }
}
```

### 2. API: `/api/auth/send-verification-code`

```typescript
catch (emailError) {
  // Детальное логирование
  logger.error('Failed to send verification email', {
    error: errorMessage,
    details: errorDetails,
    email: user.email,
    userId: user.id
  });

  // Специфичные сообщения
  let userMessage = 'Ошибка отправки email. Попробуйте позже.';
  if (errorMessage.includes('domain')) {
    userMessage = 'Ошибка конфигурации email домена.';
  } else if (errorMessage.includes('rate limit')) {
    userMessage = 'Слишком много попыток. Попробуйте через несколько минут.';
  }
  // ...
}
```

### 3. RegisterForm - Таймер повторной отправки

```typescript
const [resendTimer, setResendTimer] = useState(0);

// После успешной отправки
setResendTimer(60);
const interval = setInterval(() => {
  setResendTimer((prev) => {
    if (prev <= 1) {
      clearInterval(interval);
      return 0;
    }
    return prev - 1;
  });
}, 1000);
```

---

## 🧪 Как проверить что всё работает

### Тест 1: Проверка email обязательна для одобрения

1. Зарегистрируйте нового пользователя
2. **НЕ подтверждайте email** (просто закройте страницу)
3. Зайдите в админ-панель `/admin/users`
4. Найдите пользователя - должен быть badge "Email не подтвержден"
5. Попробуйте нажать "Одобрить" - кнопка должна быть **неактивна**
6. Если включить dev tools и попытаться вызвать API напрямую - вернёт 400 ошибку

### Тест 2: После подтверждения можно одобрить

1. Зарегистрируйте нового пользователя
2. **Подтвердите email** (введите 6-значный код)
3. Зайдите в админ-панель
4. Badge должен быть "Email подтвержден" (зелёный)
5. Кнопка "Одобрить" **активна**
6. Можно одобрить пользователя

### Тест 3: Таймер повторной отправки

1. Зарегистрируйте нового пользователя
2. На странице подтверждения нажмите "Отправить повторно"
3. Код отправлен, кнопка показывает "Повторная отправка через 60с"
4. Каждую секунду счётчик уменьшается: 59с, 58с, ...
5. Через 60 секунд кнопка снова активна

### Тест 4: Ошибки Resend

Если письмо не отправляется:
1. Проверьте Vercel Logs → Functions → `/api/auth/send-verification-code`
2. Ищите лог `Failed to send verification email`
3. В логе будет детальная информация об ошибке
4. Пользователь увидит понятное сообщение (не техническое)

---

## 🔍 Отладка проблем с Resend

### Проверьте переменные окружения

В Vercel:
```
RESEND_API_KEY = re_xxxxxxxxxxxx (должен начинаться с re_)
EMAIL_FROM = onboarding@resend.dev (без кавычек, без <...>)
```

### Проверьте логи Resend Dashboard

1. Откройте https://resend.com/logs
2. Посмотрите последние попытки отправки
3. Статусы:
   - **Delivered** ✅ - всё ок
   - **Bounced** ❌ - email не существует
   - **Failed** ❌ - проблема с конфигурацией

### Типичные ошибки

**Ошибка: "Domain not verified"**
- `EMAIL_FROM` использует неверифицированный домен
- Решение: используйте `onboarding@resend.dev`

**Ошибка: "Invalid API key"**
- `RESEND_API_KEY` неверный или истёк
- Решение: сгенерируйте новый в Resend Dashboard

**Ошибка: "Rate limit exceeded"**
- Слишком много писем за короткое время
- Решение: подождите, или увеличьте лимит в Resend

---

## 📊 Измененные файлы

1. ✅ `src/app/api/admin/users/route.ts`
   - Добавлено поле `emailVerified` в GET
   - Добавлена проверка в PATCH перед одобрением
   - Улучшена обработка ошибок

2. ✅ `src/app/admin/users/page.tsx`
   - Добавлены иконки Mail/MailCheck
   - Badge со статусом email
   - Кнопка "Одобрить" disabled если email не подтверждён
   - Показывается alert с ошибкой при неудачном обновлении

3. ✅ `src/app/api/auth/send-verification-code/route.ts`
   - Детальное логирование ошибок
   - Специфичные сообщения для разных типов ошибок
   - Debug режим для разработки

4. ✅ `src/components/auth/RegisterForm.tsx`
   - Таймер 60 секунд для повторной отправки
   - Логирование debug ошибок в консоль
   - Улучшенный текст кнопки

---

## 🚀 Следующие улучшения (опционально)

### 1. Email уведомление админу о новой регистрации
```typescript
// После регистрации отправлять админу
await resend.emails.send({
  to: 'admin@sechenov-plus.com',
  subject: 'Новая заявка на регистрацию',
  html: `Пользователь ${fullName} зарегистрировался...`
});
```

### 2. Автоматическое напоминание подтвердить email
```typescript
// Через 24 часа если email не подтвержден
if (!user.emailVerified && createdAt < 24hoursAgo) {
  sendReminder(user.email, 'Не забудьте подтвердить email');
}
```

### 3. Альтернативный способ подтверждения
- Вместо 6-значного кода использовать ссылку в email
- Клик на ссылку → автоматическое подтверждение

### 4. Тестовый режим для Resend
- В development использовать Resend Sandbox
- Не отправлять реальные письма при тестировании

---

**Текущий статус**: ✅ Все улучшения реализованы и готовы к деплою
