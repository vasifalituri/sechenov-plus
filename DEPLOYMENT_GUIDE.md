# 🚀 Руководство по публичному запуску Sechenov Plus

## 📋 Содержание
1. [Предварительная подготовка](#предварительная-подготовка)
2. [Выбор хостинга](#выбор-хостинга)
3. [Развертывание на Vercel (Рекомендуется)](#развертывание-на-vercel)
4. [Развертывание на Railway](#развертывание-на-railway)
5. [Настройка базы данных](#настройка-базы-данных)
6. [Environment Variables](#environment-variables)
7. [Post-deployment чеклист](#post-deployment-чеклист)

---

## ✅ Предварительная подготовка

### Шаг 1: Проверьте, что всё работает локально

```bash
# Запустите локально
npm run dev

# Проверьте:
✓ Регистрация/вход работает
✓ Загрузка файлов работает (до 10MB)
✓ MEGA интеграция работает
✓ База данных подключена
```

### Шаг 2: Создайте GitHub репозиторий

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit - Sechenov Plus platform"

# Создайте репозиторий на GitHub и свяжите
git remote add origin https://github.com/YOUR_USERNAME/sechenov-plus.git
git branch -M main
git push -u origin main
```

⚠️ **ВАЖНО**: Убедитесь, что `.env.local` в `.gitignore` и НЕ попал в git!

### Шаг 3: Подготовьте production базу данных

**Вариант 1: Neon (Рекомендуется для Vercel)**
- Бесплатно: https://neon.tech
- Postgres, автоматическое масштабирование
- Отлично работает с Vercel

**Вариант 2: Supabase Database**
- Бесплатно: https://supabase.com
- У вас уже есть аккаунт
- Postgres + дополнительные фичи

**Вариант 3: Railway Postgres**
- $5/месяц: https://railway.app
- Встроенная база данных

---

## 🎯 Выбор хостинга

### Сравнение платформ:

| Платформа | Цена | Сложность | Рекомендация |
|-----------|------|-----------|--------------|
| **Vercel** | Бесплатно (Hobby) | ⭐ Легко | ✅ **Лучший выбор** |
| **Railway** | $5/мес | ⭐⭐ Средне | ✅ Хорошая альтернатива |
| **Netlify** | Бесплатно | ⭐⭐ Средне | ⚠️ Ограничения для Node.js |
| **VPS (DigitalOcean)** | $6/мес | ⭐⭐⭐⭐ Сложно | ❌ Для экспертов |

### Рекомендация: **Vercel + Neon**

**Почему:**
- ✅ Бесплатно для начала
- ✅ Автоматический CI/CD из GitHub
- ✅ Глобальный CDN
- ✅ Автоматический SSL
- ✅ Отличная производительность
- ✅ Создан командой Next.js

---

## 🟢 Развертывание на Vercel (Рекомендуется)

### Шаг 1: Создайте аккаунт на Vercel

1. Перейдите: https://vercel.com
2. Нажмите **Sign Up**
3. Войдите через **GitHub** (рекомендуется)

### Шаг 2: Импортируйте проект

1. На главной странице Vercel нажмите **Add New... → Project**
2. Выберите репозиторий `sechenov-plus` из GitHub
3. Vercel автоматически определит Next.js

### Шаг 3: Настройте Build Settings

Vercel должен автоматически определить:

```
Framework Preset: Next.js
Build Command: npm run build
Output Directory: .next
Install Command: npm install
```

**ВАЖНО**: Добавьте дополнительную команду перед build:

```bash
Build Command: npx prisma generate && npm run build
```

### Шаг 4: Настройте Environment Variables

Добавьте все переменные из `.env.local`:

```env
# Database (Production)
DATABASE_URL=postgresql://user:password@neon-host/database

# NextAuth
NEXTAUTH_URL=https://your-app.vercel.app
NEXTAUTH_SECRET=generate-new-random-secret-here

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase Buckets
SUPABASE_STORAGE_BUCKET=materials
SUPABASE_PROFILES_BUCKET=profiles

# File Upload Limits
MAX_FILE_SIZE=10485760
MAX_SUPABASE_FILE_SIZE=104857600

# MEGA Storage
MEGA_EMAIL=your-mega-email@example.com
MEGA_PASSWORD=your-mega-password
MEGA_FOLDER_NAME=sechenov-plus-materials
```

⚠️ **ВАЖНО**: 
- Сгенерируйте новый `NEXTAUTH_SECRET`: 
  ```bash
  openssl rand -base64 32
  ```
- Измените `NEXTAUTH_URL` на ваш production домен

### Шаг 5: Deploy!

1. Нажмите **Deploy**
2. Vercel соберет и развернет ваше приложение
3. Получите URL: `https://sechenov-plus.vercel.app`

### Шаг 6: Запустите миграции базы данных

**После первого деплоя:**

1. Откройте Vercel Dashboard → ваш проект
2. Перейдите в **Settings → Environment Variables**
3. Найдите `DATABASE_URL` - скопируйте значение
4. На вашем локальном компьютере:

```bash
# Установите production DATABASE_URL временно
export DATABASE_URL="your-production-database-url"

# Запустите миграции
npx prisma migrate deploy

# Сгенерируйте Prisma Client
npx prisma generate

# (Опционально) Заполните базу тестовыми данными
npx prisma db seed
```

### Шаг 7: Настройте custom домен (Опционально)

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен (например: `sechenov-plus.com`)
3. Настройте DNS записи согласно инструкциям Vercel
4. Обновите `NEXTAUTH_URL` на новый домен

---

## 🚂 Развертывание на Railway

### Шаг 1: Создайте аккаунт на Railway

1. Перейдите: https://railway.app
2. Нажмите **Login with GitHub**

### Шаг 2: Создайте новый проект

1. Нажмите **New Project**
2. Выберите **Deploy from GitHub repo**
3. Выберите репозиторий `sechenov-plus`

### Шаг 3: Добавьте PostgreSQL

1. В проекте нажмите **New** → **Database** → **Add PostgreSQL**
2. Railway автоматически создаст базу и свяжет с проектом
3. `DATABASE_URL` будет автоматически добавлен в переменные

### Шаг 4: Настройте Environment Variables

Добавьте все переменные (кроме DATABASE_URL - уже есть):

```env
NEXTAUTH_URL=https://your-app.up.railway.app
NEXTAUTH_SECRET=your-secret
# ... остальные переменные
```

### Шаг 5: Настройте Build Command

1. Перейдите в **Settings**
2. В **Build Command** добавьте:
   ```bash
   npx prisma generate && npm run build
   ```
3. В **Start Command**:
   ```bash
   npm start
   ```

### Шаг 6: Deploy и миграции

Railway автоматически задеплоит. После этого:

1. Откройте Railway Dashboard → ваш проект
2. Нажмите на PostgreSQL сервис
3. Перейдите на вкладку **Connect**
4. Скопируйте `DATABASE_URL`
5. Запустите миграции локально (как в шаге 6 для Vercel)

---

## 🗄️ Настройка базы данных (Neon)

### Создание базы данных на Neon

1. Перейдите: https://neon.tech
2. **Sign Up** через GitHub
3. Создайте новый проект: **New Project**
4. Выберите регион (ближайший к пользователям)
5. Скопируйте **Connection String**

Пример:
```
postgresql://user:password@ep-xxx.region.aws.neon.tech/database?sslmode=require
```

### Запуск миграций

```bash
# Установите DATABASE_URL для production
export DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/database?sslmode=require"

# Запустите миграции
npx prisma migrate deploy

# Проверьте подключение
npx prisma studio
```

### Создание первого админа

После деплоя, вам нужно создать первого админа вручную:

**Вариант 1: Через Prisma Studio**
```bash
npx prisma studio
```
1. Откройте таблицу `User`
2. Создайте пользователя с `role: ADMIN` и `status: APPROVED`

**Вариант 2: Через SQL**
```sql
-- Подключитесь к базе через Neon Dashboard
INSERT INTO users (id, email, username, password, "fullName", "academicYear", status, role)
VALUES (
  gen_random_uuid(),
  'admin@sechenov-plus.com',
  'admin',
  '$2a$10$hashed_password', -- используйте bcrypt для хеша
  'Administrator',
  1,
  'APPROVED',
  'ADMIN'
);
```

---

## 🔐 Environment Variables (Полный список)

### Production .env

```env
# ===========================================
# DATABASE
# ===========================================
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ===========================================
# NEXTAUTH
# ===========================================
NEXTAUTH_URL="https://your-domain.com"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# ===========================================
# SUPABASE STORAGE
# ===========================================
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Supabase Buckets
SUPABASE_STORAGE_BUCKET="materials"
SUPABASE_PROFILES_BUCKET="profiles"

# ===========================================
# FILE UPLOAD LIMITS
# ===========================================
MAX_FILE_SIZE=10485760           # 10MB in bytes
MAX_SUPABASE_FILE_SIZE=104857600  # 100MB in bytes

# ===========================================
# MEGA EXTERNAL STORAGE
# ===========================================
MEGA_EMAIL="your-mega-email@example.com"
MEGA_PASSWORD="your-mega-password"
MEGA_FOLDER_NAME="sechenov-plus-materials"
```

### Как сгенерировать NEXTAUTH_SECRET

```bash
# На Mac/Linux
openssl rand -base64 32

# На Windows (PowerShell)
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))

# Или используйте онлайн:
# https://generate-secret.vercel.app/32
```

---

## ✅ Post-Deployment чеклист

### После деплоя, проверьте:

- [ ] **Сайт доступен** по URL
- [ ] **SSL работает** (https://)
- [ ] **Регистрация работает**
- [ ] **Вход работает**
- [ ] **Загрузка файлов работает** (до 10MB)
- [ ] **MEGA интеграция работает** (файлы загружаются в папку)
- [ ] **База данных работает** (пользователи сохраняются)
- [ ] **Supabase Storage работает** (изображения профилей)
- [ ] **Email уведомления работают** (если настроены)
- [ ] **Админ панель доступна** (/admin)
- [ ] **Создан первый админ** аккаунт

### Тестирование в production

```bash
# 1. Регистрация нового пользователя
# 2. Загрузка файла до 10MB
# 3. Создание обсуждения
# 4. Отправка сообщения
# 5. Добавление в закладки
# 6. Оценка материала
```

---

## 🛠️ Дополнительные настройки

### Мониторинг и логи

**Vercel:**
- Dashboard → ваш проект → **Logs**
- Realtime логи всех запросов

**Railway:**
- Dashboard → ваш проект → **Deployments** → **View Logs**

### Настройка CORS (если нужно)

В `next.config.js` уже настроены headers для CORS.

### Настройка Rate Limiting в Production

В production может потребоваться более строгий rate limiting:

```typescript
// src/lib/rate-limit.ts
const PRODUCTION_LIMITS = {
  login: { maxRequests: 5, interval: 15 * 60 * 1000 }, // 5 попыток за 15 минут
  register: { maxRequests: 3, interval: 60 * 60 * 1000 }, // 3 регистрации в час
  upload: { maxRequests: 5, interval: 24 * 60 * 60 * 1000 }, // 5 загрузок в день
};
```

### Backup базы данных

**Neon:**
- Автоматический backup каждые 24 часа (платный план)

**Railway:**
- Автоматический backup (платный план)

**Ручной backup:**
```bash
# Экспорт всей базы
pg_dump $DATABASE_URL > backup-$(date +%Y%m%d).sql

# Восстановление
psql $DATABASE_URL < backup-20260120.sql
```

---

## 🚨 Troubleshooting

### Проблема: Build fails

**Решение:**
1. Проверьте логи в Vercel/Railway
2. Убедитесь, что `npx prisma generate` запускается перед build
3. Проверьте, что все зависимости установлены

### Проблема: Database connection fails

**Решение:**
1. Проверьте `DATABASE_URL` в environment variables
2. Убедитесь, что `?sslmode=require` добавлен в URL
3. Запустите миграции: `npx prisma migrate deploy`

### Проблема: MEGA upload не работает

**Решение:**
1. Проверьте `MEGA_EMAIL` и `MEGA_PASSWORD`
2. Проверьте логи на сервере
3. Убедитесь, что `megajs` установлен в production

### Проблема: NextAuth redirect loop

**Решение:**
1. Проверьте `NEXTAUTH_URL` - должен совпадать с production URL
2. Проверьте `NEXTAUTH_SECRET` - должен быть установлен
3. Очистите cookies и попробуйте снова

---

## 📊 Стоимость

### Минимальная конфигурация (БЕСПЛАТНО):

- **Hosting**: Vercel Free Tier
- **Database**: Neon Free Tier (0.5GB)
- **Storage**: 
  - Supabase Free (1GB для профилей)
  - MEGA Free (20GB для материалов)
- **ИТОГО**: **$0/месяц** ✅

### Рекомендуемая конфигурация (для роста):

- **Hosting**: Vercel Pro ($20/мес)
- **Database**: Neon Scale ($19/мес)
- **Storage**: 
  - Supabase Pro ($25/мес)
  - MEGA Pro ($5.5/мес)
- **ИТОГО**: **$69.5/месяц**

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен публично!

### Поделитесь ссылкой:

```
🌐 Sechenov Plus
   https://your-domain.com
   
📚 Образовательная платформа для медицинских студентов
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи в Vercel/Railway Dashboard
2. Проверьте Environment Variables
3. Проверьте подключение к базе данных
4. Проверьте MEGA credentials

---

**Создано**: 20.01.2026  
**Версия**: 1.0  
**Платформа**: Next.js 16.1.3 + Prisma + PostgreSQL + MEGA
