# 🚀 Двухэтапная загрузка файлов в MEGA

## Проблема
Vercel имеет жёсткий лимит **4.5MB на размер тела запроса**, что делает невозможной прямую загрузку файлов в MEGA через Vercel API.

## ✅ Решение: Двухэтапная загрузка

### Как это работает:

```
Пользователь → Supabase (прямая загрузка) → MEGA (автоматический перенос) → Удаление из Supabase
```

### Этап 1: Загрузка в Supabase (мгновенно)
1. Пользователь выбирает файл на странице `/materials/upload`
2. Файл загружается **напрямую с браузера в Supabase Storage**
3. Метаданные сохраняются в базу данных
4. Пользователь видит: "Материал загружен успешно!"

### Этап 2: Перенос в MEGA (автоматически, в фоне)
1. Сразу после загрузки запускается фоновая задача
2. Файл скачивается из Supabase на Vercel Function
3. Файл загружается в MEGA через API
4. URL в базе данных обновляется на MEGA URL
5. Файл удаляется из Supabase (освобождается место)

### Результат:
- ✅ Пользователь не ждёт долгую загрузку в MEGA
- ✅ Обходится лимит Vercel 4.5MB
- ✅ Все файлы в итоге хранятся в MEGA (20GB бесплатно)
- ✅ Supabase используется только как временное хранилище

---

## 📁 Архитектура

### Новые файлы:

**1. `/api/materials/migrate-to-mega` - API для миграции**
- POST: Переносит файл из Supabase в MEGA
- GET: Проверяет статус миграции
- Доступен только админам

**2. Обновлённый `/api/materials` - Автоматический триггер**
- После создания материала автоматически запускает миграцию
- Fire-and-forget подход (не блокирует ответ)

**3. Обновлённая админ-панель**
- Показывает иконку хранилища (Supabase/MEGA)
- Кнопка для ручного переноса в MEGA
- Индикатор статуса миграции

---

## 🎯 Как использовать

### Для пользователей:

1. Откройте `/materials/upload`
2. Заполните форму и выберите файл (до 10MB)
3. Нажмите "Загрузить материал"
4. Файл загрузится мгновенно! ✅
5. Через несколько минут файл автоматически переместится в MEGA

**Пользователь ничего не замечает** - всё работает автоматически!

### Для администраторов:

1. Откройте `/admin/materials`
2. Увидите badge с типом хранилища:
   - 🟦 **Supabase** - файл ещё в Supabase
   - 🟪 **MEGA** - файл в MEGA
3. Если файл в Supabase, можно вручную перенести:
   - Нажмите кнопку ☁️ (Cloud icon)
   - Подтвердите перенос
   - Дождитесь завершения

---

## 🔧 Настройка

### Переменные окружения (Vercel):

```env
# MEGA Storage (обязательно)
MEGA_EMAIL=your-email@mega.nz
MEGA_PASSWORD=your-password
MEGA_FOLDER_NAME=sechenov-plus-materials

# Supabase (обязательно)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### Проверка настройки MEGA:

```bash
# Локально (в корне проекта)
npm run dev
```

Попробуйте загрузить тестовый файл через админ-аккаунт.

---

## 📊 Мониторинг

### Проверить статус миграции:

**GET** `/api/materials/migrate-to-mega?materialId=xxx`

Ответ:
```json
{
  "success": true,
  "data": {
    "materialId": "xxx",
    "fileName": "test.pdf",
    "storageType": "EXTERNAL_MEGA",
    "inMega": true,
    "externalUrl": "https://mega.nz/..."
  }
}
```

### Логи в Vercel:

1. Vercel Dashboard → Deployments → Functions
2. Найдите `/api/materials/migrate-to-mega`
3. Посмотрите логи выполнения

Типичные логи:
```
✅ Starting migration to MEGA
✅ File downloaded from Supabase
✅ File uploaded to MEGA
✅ File deleted from Supabase
```

---

## ⚠️ Важные детали

### Время переноса:
- Файл 1MB: ~30 секунд
- Файл 5MB: ~2 минуты
- Файл 10MB: ~5 минут

### Что если миграция упала?
- Файл остаётся в Supabase (ничего не потеряется)
- Админ может запустить миграцию вручную
- Или можно повторить автоматически через cron job

### Лимиты:
- **Supabase Free**: 1GB хранилища (временное)
- **MEGA Free**: 20GB хранилища (финальное)
- **Vercel Function**: до 5 минут выполнения (достаточно для 10MB)

---

## 🔄 Ручная миграция всех файлов

Если хотите перенести все существующие файлы из Supabase в MEGA:

### Создайте скрипт:

```typescript
// scripts/migrate-all-to-mega.ts
import { prisma } from '@/lib/prisma';

async function migrateAll() {
  const materials = await prisma.material.findMany({
    where: { storageType: 'SUPABASE' },
  });

  console.log(`Found ${materials.length} files to migrate`);

  for (const material of materials) {
    console.log(`Migrating: ${material.fileName}`);
    
    const response = await fetch('http://localhost:3000/api/materials/migrate-to-mega', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ materialId: material.id }),
    });

    const result = await response.json();
    if (result.success) {
      console.log(`✅ ${material.fileName}`);
    } else {
      console.error(`❌ ${material.fileName}: ${result.error}`);
    }

    // Wait 30 seconds between migrations
    await new Promise(resolve => setTimeout(resolve, 30000));
  }

  console.log('Migration completed!');
}

migrateAll();
```

**Запуск:**
```bash
npx ts-node scripts/migrate-all-to-mega.ts
```

---

## 🎉 Преимущества этого подхода

### ✅ Для пользователей:
- Мгновенная загрузка (не нужно ждать MEGA)
- Надёжность (Supabase быстрее и стабильнее)
- Прозрачность (пользователь не знает о двух этапах)

### ✅ Для админов:
- Видимость (badge показывает где файл)
- Контроль (можно мигрировать вручную)
- Гибкость (можно оставить файлы в Supabase при необходимости)

### ✅ Технически:
- Обходит лимит Vercel 4.5MB
- Экономит место в Supabase (файлы удаляются)
- Использует сильные стороны обоих сервисов
- Асинхронная обработка (не блокирует пользователя)

---

## 🚀 Альтернативы (для будущего)

Если MEGA перестанет работать или нужно больше контроля:

### Вариант 1: AWS S3 + CloudFront
- Presigned URLs для прямой загрузки
- Нет прохождения через Vercel
- ~$0.023/GB/месяц

### Вариант 2: Cloudflare R2
- S3-совместимый API
- Бесплатно до 10GB
- Нет платы за исходящий трафик

### Вариант 3: Vercel Blob Storage
- Встроенное решение для Vercel
- До 100MB на файл
- $0.15/GB/месяц

---

## 📝 Changelog

**v1.0** (текущая версия)
- ✅ Двухэтапная загрузка Supabase → MEGA
- ✅ Автоматический перенос после загрузки
- ✅ Ручной перенос через админ-панель
- ✅ Индикатор типа хранилища
- ✅ API для проверки статуса

---

**Текущий статус**: ✅ Готово к деплою
