# 📊 Инструкция по применению миграции для голосований

## ⚠️ ВАЖНО: Нужно применить миграцию базы данных!

После деплоя на Vercel выполните следующие шаги:

### Шаг 1: Создайте миграцию локально

```bash
npx prisma migrate dev --name add_polls
```

Это создаст файл миграции в папке `prisma/migrations/`

### Шаг 2: Примените миграцию на продакшене

Вариант А - через Vercel CLI:
```bash
vercel env pull .env.local
npx prisma migrate deploy
```

Вариант Б - через Neon Dashboard:
1. Откройте https://console.neon.tech
2. Выберите ваш проект
3. Перейдите в SQL Editor
4. Скопируйте и выполните SQL из созданного файла миграции

### Шаг 3: Сгенерируйте Prisma Client

```bash
npx prisma generate
```

### Шаг 4: Перезапустите Vercel

```bash
vercel --prod
```

---

## 📋 Что было добавлено:

### Таблицы:
- `polls` - Голосования (вопрос, описание, активность)
- `poll_options` - Варианты ответов
- `poll_votes` - Голоса пользователей

### Связи:
- User → Poll (createdPolls)
- User → PollVote (pollVotes)
- Poll → PollOption (options)
- Poll → PollVote (votes)

---

## 🎯 Как использовать:

### Для админа/модератора:
1. Зайдите в админ-панель: `/admin/polls`
2. Нажмите "Создать голосование"
3. Введите вопрос и варианты ответов
4. Голосование появится на главной странице

### Для пользователей:
1. Откройте главную страницу
2. Голосование появится после объявлений
3. Выберите вариант ответа
4. Увидите результаты с процентами

---

## 🐛 Если возникли проблемы:

### Ошибка: "Table 'polls' doesn't exist"
- Миграция не применена
- Выполните: `npx prisma migrate deploy`

### Ошибка: "Cannot find module '@prisma/client'"
- Не сгенерирован Prisma Client
- Выполните: `npx prisma generate`

### Голосование не появляется на главной
- Создайте голосование в админ-панели
- Проверьте что оно активное (isActive = true)

---

## ✅ Проверка работы:

1. Создайте тестовое голосование
2. Откройте главную страницу
3. Проголосуйте
4. Обновите страницу - голос должен сохраниться
5. Проверьте результаты в админ-панели
