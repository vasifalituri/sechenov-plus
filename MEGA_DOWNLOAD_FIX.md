# 🔗 Исправление скачивания из MEGA

## Проблема
При скачивании файлов из MEGA пользователь оставался на сайте, а файл скачивался через сервер. Нужно было открывать страницу MEGA в новой вкладке.

## ✅ Решение

### Изменения в архитектуре:

**Раньше:**
```
Пользователь → Клик "Скачать" → Server redirect → Файл скачивается через сайт
```

**Теперь:**
```
Пользователь → Клик "Скачать" → API возвращает MEGA URL → Открывается в новой вкладке
```

---

## 📁 Изменённые файлы:

### 1. `/api/materials/download/[id]/route.ts`
**Изменение:** Вместо `NextResponse.redirect()` возвращаем JSON с MEGA URL

```typescript
// Раньше:
return NextResponse.redirect(material.externalUrl);

// Теперь:
return NextResponse.json({
  success: true,
  downloadType: 'EXTERNAL_MEGA',
  externalUrl: material.externalUrl,
  fileName: material.fileName,
});
```

### 2. `src/components/materials/DownloadButton.tsx` (новый)
**Клиентский компонент** для обработки скачивания:
- Проверяет тип ответа (JSON = MEGA, blob = Supabase/Local)
- Для MEGA: открывает в новой вкладке через `window.open()`
- Для Supabase/Local: скачивает файл через blob URL
- Показывает разные иконки: 🔗 для MEGA, 📥 для обычных файлов

### 3. `/materials/[id]/page.tsx`
**Изменение:** Заменили `<a href>` на `<DownloadButton>`
- Старая кнопка использовала прямую ссылку
- Новая кнопка использует JavaScript для обработки разных типов файлов

### 4. `/admin/materials/page.tsx`
**Изменение:** Добавлена функция `handleDownload()`
- Кнопка скачивания теперь показывает разные иконки
- 🔗 `ExternalLink` для MEGA файлов
- 📥 `Download` для Supabase/Local файлов

---

## 🎯 Как работает:

### Для MEGA файлов:

1. Пользователь кликает "Скачать с MEGA"
2. Клиент запрашивает `/api/materials/download/{id}`
3. API возвращает JSON:
   ```json
   {
     "success": true,
     "downloadType": "EXTERNAL_MEGA",
     "externalUrl": "https://mega.nz/file/xxx#yyy",
     "fileName": "lecture.pdf"
   }
   ```
4. `DownloadButton` открывает URL в новой вкладке
5. Пользователь видит страницу MEGA с кнопкой "Download"

### Для Supabase/Local файлов:

1. Пользователь кликает "Скачать материал"
2. Клиент запрашивает `/api/materials/download/{id}`
3. API возвращает файл (blob)
4. `DownloadButton` создаёт временный URL и скачивает файл
5. Файл автоматически сохраняется на компьютер

---

## 🎨 UI изменения:

### Кнопка скачивания на странице материала:

**MEGA файл:**
```
┌──────────────────────────────┐
│ 🔗 Скачать с MEGA            │
└──────────────────────────────┘
```

**Обычный файл:**
```
┌──────────────────────────────┐
│ 📥 Скачать материал          │
└──────────────────────────────┘
```

### В админ-панели:

**MEGA файл:** Иконка 🔗 (ExternalLink) + tooltip "Открыть в MEGA"
**Обычный файл:** Иконка 📥 (Download) + tooltip "Скачать"

---

## ✅ Преимущества нового подхода:

1. **Для MEGA файлов:**
   - ✅ Открывается в новой вкладке (не покидаем сайт)
   - ✅ Пользователь видит страницу MEGA
   - ✅ Не нагружает сервер (прямая ссылка)
   - ✅ Поддержка больших файлов без таймаута

2. **Для Supabase/Local файлов:**
   - ✅ Автоматическое скачивание (как раньше)
   - ✅ Работает без JavaScript (fallback на `<a download>`)
   - ✅ Совместимость со старыми браузерами

3. **Универсально:**
   - ✅ Один компонент для всех типов файлов
   - ✅ Визуальная индикация типа хранилища
   - ✅ Понятный UX для пользователя

---

## 🧪 Тестирование:

### Проверка MEGA скачивания:

1. Загрузите файл через `/materials/upload`
2. Дождитесь автоматической миграции в MEGA (1-2 минуты)
3. В админ-панели убедитесь, что badge показывает "MEGA"
4. Откройте страницу материала `/materials/{id}`
5. Кликните "Скачать с MEGA"
6. **Ожидаемый результат:**
   - Откроется новая вкладка с страницей MEGA
   - На странице MEGA будет кнопка "Download"
   - Файл можно скачать с MEGA

### Проверка Supabase скачивания:

1. Найдите файл с badge "Supabase" (если нет - остановите миграцию)
2. Откройте страницу материала
3. Кликните "Скачать материал"
4. **Ожидаемый результат:**
   - Файл начнёт скачиваться автоматически
   - Без открытия новых вкладок

---

## ⚠️ Возможные проблемы:

### Проблема: MEGA ссылка не открывается

**Причина:** Popup blocker в браузере
**Решение:** Разрешите popup для вашего сайта

### Проблема: "Unexpected token" при скачивании

**Причина:** API возвращает HTML вместо JSON/blob
**Решение:** Проверьте логи Vercel, возможно ошибка аутентификации

### Проблема: Файл скачивается через сайт (старое поведение)

**Причина:** Кэш браузера
**Решение:** Очистите кэш (Ctrl+Shift+R) или откройте в режиме инкогнито

---

## 🔄 Обратная совместимость:

Старые прямые ссылки `/api/materials/download/{id}` **всё ещё работают**:
- Для MEGA файлов возвращают JSON (нужен JavaScript для обработки)
- Для Supabase/Local файлов возвращают blob (работает как раньше)

Если пользователь откроет `/api/materials/download/{id}` напрямую в адресной строке:
- MEGA файл: увидит JSON ответ
- Supabase/Local: файл начнёт скачиваться

---

## 📊 Статистика:

Все скачивания отслеживаются одинаково (MEGA и Supabase):
- Счётчик увеличивается при запросе к API
- Уникальные скачивания на пользователя
- Обновление `downloadCount` в базе данных

---

## 🚀 Следующие шаги (опционально):

### Улучшение 1: Показывать информацию о файле перед MEGA

Добавить модальное окно перед открытием MEGA:
```
┌─────────────────────────────────┐
│ Вы будете перенаправлены на     │
│ MEGA для скачивания файла       │
│                                 │
│ Файл: lecture.pdf               │
│ Размер: 5.2 MB                  │
│                                 │
│ [Отмена]  [Открыть MEGA]        │
└─────────────────────────────────┘
```

### Улучшение 2: Встроенный просмотр MEGA

Использовать MEGA embed для просмотра PDF прямо на сайте:
```html
<iframe src="https://mega.nz/embed/file#key"></iframe>
```

### Улучшение 3: Альтернативная ссылка

Добавить кнопку "Скачать напрямую" для скачивания через прокси (медленнее, но без MEGA страницы).

---

**Текущий статус**: ✅ Готово к деплою
