# 📧 Инструкция по исправлению EMAIL_FROM

## Проблема
В Vercel была установлена переменная `EMAIL_FROM` со значением:
```
EMAIL_FROM="Sechenov+ <onboarding@resend.dev>"
```

Это вызывало ошибки при отправке писем через Resend.

## ✅ Решение

### Шаг 1: Изменить переменную в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект **sechenov-plus**
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите переменную `EMAIL_FROM`
5. Нажмите **Edit** (карандаш справа)
6. Измените значение на:
   ```
   onboarding@resend.dev
   ```
   ⚠️ **Важно**: Без кавычек, без названия "Sechenov+", без угловых скобок `< >`

7. Нажмите **Save**

### Шаг 2: Передеплоить проект

После изменения переменной окружения нужно передеплоить проект:

**Вариант A: Через Git Push**
```bash
git add .
git commit -m "fix: update EMAIL_FROM configuration"
git push
```

**Вариант B: Через Vercel Dashboard**
1. Перейдите в **Deployments**
2. Найдите последний успешный деплой
3. Нажмите **⋯** (три точки) → **Redeploy**

### Шаг 3: Проверить

После деплоя протестируйте:

1. Откройте https://sechenov-plus.vercel.app
2. Перейдите на страницу регистрации
3. Зарегистрируйте нового пользователя
4. Проверьте почту - должно прийти письмо с кодом подтверждения

### Шаг 4: Проверить логи (если нужно)

Если письмо не пришло:
1. В Vercel Dashboard → **Deployments** → кликните на последний деплой
2. Перейдите во вкладку **Functions**
3. Найдите `/api/auth/register` или `/api/auth/send-verification-code`
4. Посмотрите логи на наличие ошибок

## 📝 Что было изменено в коде

В репозитории обновлены следующие файлы с правильным значением `EMAIL_FROM`:
- `.env.example`
- `README.md`
- `DEPLOYMENT_GUIDE.md`

Код в `src/app/api/auth/send-verification-code/route.ts` и `src/app/api/auth/register/route.ts` уже правильно использует переменную окружения.

## ℹ️ Почему это работает?

- `onboarding@resend.dev` - это специальный тестовый домен Resend
- Он уже верифицирован и готов к использованию
- Не требует настройки DNS записей
- Подходит для тестирования и небольших проектов

## 🚀 Для продакшена (будущее)

Для профессионального использования рекомендуется:
1. Купить собственный домен (например, `sechenov-plus.com`)
2. Добавить домен в [Resend Dashboard → Domains](https://resend.com/domains)
3. Настроить DNS записи (SPF, DKIM, DMARC)
4. Использовать свой email: `noreply@sechenov-plus.com`

---

**Текущий статус**: ✅ Документация обновлена, осталось изменить переменную в Vercel
